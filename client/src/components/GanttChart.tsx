import React from 'react';
import type { GanttChartProps } from '../types';

const GanttChart: React.FC<GanttChartProps> = ({ tasks, onTaskDateChange, onTaskSelect }) => {
  // TODO: 外部ライブラリでガントチャート表示
  return <div>ガントチャート表示領域</div>;
};

export default GanttChart;
