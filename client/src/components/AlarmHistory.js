import React from "react";
import useScreenTitle from "./useScreenTitle";

function AlarmHistory() {
    useScreenTitle("アラーム履歴");
    return <div>アラーム履歴画面（ダミー）</div>;
}

export default AlarmHistory;
