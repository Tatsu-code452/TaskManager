import { defaultPayloadFor, SAMPLE_PRESETS } from "../utils/demoUtils";
import { Entity } from "../const/demoConst";

export const useEntityPresets = () => {
    return {
        defaultPayloadFor,
        SAMPLE_PRESETS,
    };
};