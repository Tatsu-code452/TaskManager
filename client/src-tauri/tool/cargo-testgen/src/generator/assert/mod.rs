pub mod model;

// use serde_json::Value;

// pub enum AssertMode {
//     Model,
//     // Db,
//     // Service,
//     // Command,
// }

// pub fn generate_asserts(mode: AssertMode, expected: &Value) -> Vec<String> {
//     match mode {
//         AssertMode::Model => model::generate_asserts(expected),
//         // AssertMode::Db => db::generate_db_asserts(expected),
//         // AssertMode::Service => service::generate_service_asserts(expected),
//         // AssertMode::Command => command::generate_command_asserts(expected),
//     }
// }
